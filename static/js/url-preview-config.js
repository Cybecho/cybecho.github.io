// URL Preview Configuration (DISABLED)
window.URLPreviewConfig = {
  // DISABLED - All links shown as simple hyperlinks for LLVM style
  enabled: false
};

console.log('URL Preview configuration disabled');
